import type { NextPage } from "next";
import { SignUp } from "../components";

const LogUp: NextPage = () => {
    return (
        <div className="container">
            <SignUp />
        </div>
    );
};

export default LogUp;